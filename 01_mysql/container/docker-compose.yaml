services:
  eadb:
    container_name: eadb
    image: mariadb:latest
    ports:
      - 3301:3306
    volumes:
      - ./volumes/initdb.d:/docker-entrypoint-initdb.d
    environment:
      - MYSQL_ROOT_PASSWORD=password
